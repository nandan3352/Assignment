// first i should know grouping/ordering ==>so variable is enough(<=3 for grouping)
// if i know above Number, i should call corresponding cards ==>2 different handler functions
// mostly card is same for all 5 types, so try to see differences and proceed
// colour to rgb converter and vice versa are required
// svg's and api stuff
// state lifting, so should maintain states in app.js to give to sibling components